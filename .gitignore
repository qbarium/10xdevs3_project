# 10xDevs licensed skill content — installed locally via 10x-cli, not for public redistribution.
# Skills are copied manually into projects that need them; metadata files (lock, manifest) stay tracked.
.claude/skills/
.agents/skills/

# from 10x-astro-starter
# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# cloudflare
.dev.vars
.wrangler/

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# local-only docs (session handoff, personal notes — never pushed)
docs/local/
